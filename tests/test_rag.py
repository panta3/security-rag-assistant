# TODO: real tests once ingestion/embeddings/vectorstore/rag are implemented.
# Suggestion: use a couple of tiny synthetic PDFs as fixtures rather than
# real security docs, so tests run fast and deterministically.


def test_placeholder():
    assert True
